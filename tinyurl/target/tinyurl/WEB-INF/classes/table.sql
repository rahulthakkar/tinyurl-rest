CREATE  TABLE url_mapping (id INTEGER PRIMARY KEY  AUTOINCREMENT  NOT NULL , hash TEXT NOT NULL  UNIQUE , long_url TEXT NOT NULL , created_at DATETIME DEFAULT CURRENT_TIMESTAMP);

CREATE UNIQUE INDEX hashstr_index ON url_mapping (hash ASC);
